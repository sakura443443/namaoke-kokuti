# 生バンドカラオケ 自動告知エージェント

毎週水曜の「生バンドカラオケ」の告知を、**毎週金曜11時に Instagram と Facebook へ自動投稿**する
仕組みです。GitHub Actions の上で動きます。全体像は [README.md](README.md) にあります。

さくらさん向けの準備の手順は [セットアップ手順.md](セットアップ手順.md) です。

## ここで守ること

### 本文・料金・時間を勝手に変えない ★重要

告知文のひな形は `scripts/build_draft.py` の `render()` にあります。
文体（です・ます調／`■` を使わない）は `../koukoku/CLAUDE.md` に従います。
**変えるときは必ずさくらさんに確認します。**

ただし、この毎週の告知だけは `../koukoku/CLAUDE.md` と違う点が2つあります
（2026-09-02 にさくらさんから指示。**元に戻してはいけません**）。

- **電話番号・DM の予約案内を入れません。** 毎週の定例なので、予約を促す形にしていません
- **締めの「ご予約お待ちしてます。」を入れません。** ライブ告知の決まりですが、ここでは使いません

ハッシュタグは `data/config.json` の `hashtags` です。
**「#」は必ず半角にします。** 全角の「＃」はタグとして働きません。

料金・時間・曲数は `data/config.json` が原典です。ポスターの記載と合っています。
**思いつきで書き換えないでください。** 印刷物と食い違うと事故になります。

### メンバー表は2つあります

- `data/hosts.json` … 自動投稿が見る原典（アカウント名も持つ）
- `../koukoku/生バンドカラオケ-ホストメンバー.md` … 人が読む表

**どちらか片方だけ直してはいけません。** 必ず両方を同じにします。

### トークンを扱わない ★重要

`META_PAGE_TOKEN` は、お店のアカウントで投稿できる鍵です。

- **チャットに貼ってもらってはいけません。** 会話の記録に残ります
- ファイルに書きません。GitHub の Secrets にだけ置きます
- `scripts/setup_token.py` は、さくらさんが自分のターミナルで動かします

### 投稿を試すときは、まず消えるものから

本番のアカウントに出ます。取り消せません。

```bash
python3 scripts/post.py --draft drafts/2026-09-09.json --dry-run   # 投稿しない
python3 scripts/post.py --draft drafts/2026-09-09.json --only story # 24時間で消える
```

**`--dry-run` を通してから**でないと、本番を叩かないでください。

### 承認の仕組みを弱めない ★重要

金曜の投稿は、Issue に持ち主が `approve` とコメントしたときだけ実行されます。
リポジトリは公開なので、**持ち主以外のコメントは承認として扱いません**
（`scripts/github_issue.py` の `owner()` 判定）。ここを緩めてはいけません。

## Meta の API について、確かめてあること

2026年9月2日に確認しました。

- Instagram のストーリーズは **API なら投稿できます**（`media_type=STORIES`）。
  できないのは instagram.com の画面からです
- **ストーリーズには本文・位置情報・タグ付けを付けられません。** フィードには付きます
- Facebook は**ページにしか**投稿できません（個人プロフィールは2018年に廃止）
- Instagram は画像を**公開URLからしか**受け取りません。だから画像を GitHub に置いています
- Facebook のストーリーは `published=false` で写真を上げてから `/photo_stories` に渡します

**確かめていないことを「できます」と言ってはいけません。**
分からないときは調べるか、分からないと言ってください。
